import { describe, expect, it } from 'vitest'
import CounterWidget from './CounterWidget.vue'

describe('CounterWidget', () => {
  it('must be defined', () => {
    expect(CounterWidget).toBeTruthy()
  })
})
